<% @books.each do |book| %>
  <%= link_to book_path(book.id) do %>
    Title:<%= book.title %><br>
  <% end %>  
  Opinion:<%= book.body %><br>
  <%= image_tag book.get_image, class: "resize-image" %>
<% end %>  

<%= render "new", book: @book %>
